import {
    StyleSheet
  } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1, 
        justifyContent: "center",
        alignItems: 'center',
        padding: 20,
    },
    logo: {
        fontSize: 24,
        fontWeight: 'bold',
        marginBottom: 20,
    },
    description: {
        marginBottom: 40,
    },
    form: {
        alignItems: 'center',
    },
    formField: {
        flexDirection: "row",
        marginBottom: 20,
    },
    input: {
        width: "80%",
        marginLeft: 10,
        marginRight: 10,
    },
    button: {
        
    }
    
  });
  
export default styles;