import {
    StyleSheet
  } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1, 
        justifyContent: "center",
        alignItems: 'center',
    },
    logo: {
        fontSize: 24,
        fontWeight: 'bold',
        fontFamily: 'Khula',
        fontWeight: 'normal',
    },
    form: {
        alignItems: 'center',
    },
    input: {

    },
    button: {
        
    }
    
  });
  
export default styles;